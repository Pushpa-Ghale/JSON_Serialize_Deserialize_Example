# JSON_Serialize_Deserialize_Example

This is just a simple example that does serializes and deserializes a JSON string to and from an custom Employee Object.
I am using newtonsoft library to do the conversion.
